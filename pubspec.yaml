name: my_gestor
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: "3.6.1"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.8.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true